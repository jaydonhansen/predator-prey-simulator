1. To generate a makefile, run "cmake CMakeLists.txt".
2. To build the application, run "make".
3. To run the application, run "./milestone1".

The parameters are hard-coded in Main.cpp, so you can change them if you wish,
but future implementations will feature command-line arguments.
